// Import the Firebase Admin SDK
const admin = require('firebase-admin');

// Initialize Firebase Admin SDK with default credentials (service account)
admin.initializeApp();

// You can now use admin SDK to interact with Firebase services (e.g., Firebase Authentication, Firestore, etc.)
console.log("Firebase Admin SDK initialized successfully");
